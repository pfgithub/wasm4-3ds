// walls will be two blocks thick
// a small window is ~6 blocks

// we can make a plctfarmer engine : pixel art editor + entity editor = plctfarmer engine
// can also do multi layer for 3d

const Grid = struct {

};
const GridView = struct {};

pub fn main() !void {

}
